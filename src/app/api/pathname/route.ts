/** @format */

// /** @format */

// import { NextRequest, NextResponse } from "next/server";

// export async function GET(request: NextRequest) {
//   try {
//     const url = request.url;
//     const pathname = new URL(url).pathname;
//     return NextResponse.json({ pathname: pathname });
//   } catch (error) {
//     throw error;
//   }
// }
